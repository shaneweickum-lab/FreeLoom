__pycache__/
.pytest_cache/
*.pyc
checkpoints/
data/prepared/
data/base_corpus/

# export_web_weights.py's local output -- staging only. Real output gets
# copied into wherever the Next.js app actually bundles it from (decided once
# the TS runtime lands), not committed from here directly.
serve/web_weights/

# freeloom_scraper.py's output -- a research artifact reviewed and converted
# to SQL by hand each time, not something to keep re-committing wholesale.
scripts/*.csv
scripts/*.xlsx

# convert_research_spreadsheet.py's output -- a generated migration applied
# directly against Supabase, not app source, so it isn't committed here.
scripts/*.sql
